import { mockGetFrameAnimation } from './animations';

const mockConfig = {
    stateChanged: mockGetFrameAnimation,
    selectedFrameLayout: mockGetFrameAnimation,
    selectedFrameContent: mockGetFrameAnimation,
    editorLink: 'https://chili-editor-dev.azurewebsites.net/',
    getFrameAnimation: mockGetFrameAnimation,
};
export default mockConfig;
