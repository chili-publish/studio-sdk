{
  "name": "actions",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "yarn tsc && yarn tsc -p tsconfig.types.json && cp types/Actions.d.ts out/Actions.d.ts && node scripts/compress.mjs"
  },
  "devDependencies": {
    "openai": "^4.8.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "terser": "^5.19.4",
    "typescript": "^5.2.2"
  }
}
