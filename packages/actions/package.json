{
  "license": "MIT",
  "name": "grafx-studio-actions",
  "version": "0.0.1",
  "devDependencies": {
    "eslint": "^8.48.0",
    "typescript": "^5.2.2",
    "prettier": "^3.0.3"
  },
  "scripts": {
    "build": "yarn run tsc --build --verbose",
    "start": "yarn run tsc --watch",
    "clean": "rm -rf dist",
    "ci-lint": "eslint \"./src/**/*.{js,jsx,ts,tsx,json}\"",
    "test": "echo 'no tests yet'",
    "cover": "echo 'no tests yet'",
    "format": "prettier --verbose --write \"./src/**/*.{js,jsx,ts,tsx,json,css,scss,md}\"",
    "format-staged": "pretty-quick --staged",
    "publish": "echo 'no publish yet'"
  }
}
